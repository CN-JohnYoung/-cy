# 1. 全局变量
全局变量的命名应当具有前缀，其中全局变量为gv_，全局函数为gf_，全局结构体应为gs_，全局常量应为gc_。

对于局部变量，将g换为l。对于函数参数，将g换为p

# 2. 数组声明
对于galaxy来说，数组长度也是数据类型的一员，通常来说数组下标0不会正式使用，可用作边界判定等用途，声明数组时自动在后面+1，且数组长度应放到类型后面，如int[length + 1] a;